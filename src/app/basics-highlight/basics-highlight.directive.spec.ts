import { BasicsHighlightDirective } from './basics-highlight.directive';

describe('BasicsHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BasicsHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
